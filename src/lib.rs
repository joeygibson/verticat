pub fn process_file(
    input: &str,
    count: bool,
    head: Option<i32>,
    tail: Option<i32>,
    force: bool,
) -> Result<(), String> {
    println!(
        "input: {:?}, count: {}, head: {:?}, tail: {:?}, force: {}",
        input, count, head, tail, force
    );

    


    return Ok(());
}
